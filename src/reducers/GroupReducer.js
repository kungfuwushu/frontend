import { ActionType } from './../actions/Helpers';

const initialGroups = [
    {
        name: 'Groupe 1'
    },
    {
        name: 'Groupe 2'
    }
]

export const GroupReducer =(state = initialGroups, action)=>{
    return state;
}
